# Dataset artifacts — large, regenerated by manual_data_collector.py. Do NOT commit.
dataset/IMG/
dataset/*.csv

# Webots world backups (we keep the patched .wbt, not the .bak)
worlds/*.bak

# Python
__pycache__/
*.pyc

# Smoke-test and training artifacts — large model files, not for git
notebooks/artifacts_*/
notebooks/*.h5
notebooks/*.weights.h5

# Demo videos — large, hosted on YouTube for submission, not in git
video_evidence/*.mp4
